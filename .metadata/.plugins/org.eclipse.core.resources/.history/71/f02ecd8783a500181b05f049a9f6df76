package com.scripteasy.services;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Arrays;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.stereotype.Service;

import com.scripteasy.domain.User;
import com.scripteasy.domain.enums.Profile;
import com.scripteasy.repositories.UserRepository;


@Service
public class DBService {

	@Autowired
	private BCryptPasswordEncoder pe;

	private UserRepository userRepository;
	@Autowired


	public void IstantiateTestDataBase() throws ParseException {
		

		User us1 = new User (null, "Maria Silva", "maria@gmail.com",  pe.encode("123"));
		us1.getPhones().addAll(Arrays.asList("27736323", "93838393"));
		
		User us2 = new User(null, "Ana Costa", "ana@gmail.com", pe.encode("123"));
		us1.getPhones().addAll(Arrays.asList("99336323", "93838258"));
		us2.addProfile(Profile.ADMIN);




		userRepository.saveAll(Arrays.asList(us1, us2));

	}

}
